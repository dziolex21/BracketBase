import 'package:flutter/material.dart';

class TournamentSettings extends StatelessWidget {
  const TournamentSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Tournament Settings'),
      ),
      body: const Center(
        child: Text('Tournament Settings Placeholder'),
      ),
    );
  }
}
